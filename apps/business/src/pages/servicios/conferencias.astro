---
import Site from "../../layouts/Site.astro";
import PromesaTicketplus from "../../components/PromesaTicketplus.astro";
import EquipoSection from "../../components/EquipoSection.astro";

// 3 momentos del ciclo del congreso (registro / evento / ROI)
const moments = [
  {
    n: "01", tag: "EL REGISTRO", dark: false, id: "registro",
    title: "Buscas registrar C-level, sponsors, prensa y ponentes con flows distintos.",
    lead: "Encuentras forms dinámicos + acreditación profesional + multi-moneda. Tu congreso registra ejecutivos C-level, sponsors, prensa especializada y staff. Cada uno con su flow, su credencial y sus permisos.",
    heroCount: 0,
    features: [
      { n: "01", title: "Formularios de registro dinámicos por categoría", body: "Forms condicionales que cambian según la categoría seleccionada. Validación por RUT empresa, dominio corporativo o credencial de medio.", badge: "Forms condicionales — único en CL" },
      { n: "02", title: "Multi-moneda CLP + USD para ponentes internacionales", body: "Stripe Connect nativo. Cobro en CLP al fan local, en USD al asistente internacional, liquidación al ponente en USD a su cuenta extranjera. Compliance Banco Central Chile resuelto.", badge: null },
      { n: "03", title: "Exhibitor management — panel propio por stand", body: "Cada expositor con login propio: stock de invitaciones, pre-acreditación de staff por tipo, permisos por paquete gold/silver/bronze y reportes de tráfico por stand.", badge: "Panel propio por stand" },
    ],
  },
  {
    n: "02", tag: "EL DÍA DEL EVENTO", dark: true, id: "evento",
    title: "Buscas que el congreso sea plataforma de negocios, no auditorio de PowerPoint.",
    lead: "Encuentras Netlink + check-in por sesión + dashboards configurables. El cliente corporativo paga por el contenido, pero vuelve el próximo año por el networking.",
    heroCount: 0,
    features: [
      { n: "01", title: "Check-in QR por sesión", body: "Certificación de asistencia con QR único por sesión. Créditos de capacitación, engagement por charla, NPS opcional por sesión.", badge: null },
      { n: "02", title: "Dashboards configurables para el cliente", body: "Personalizables con la marca del cliente (cámara, asociación gremial, corporativo). El sponsor ve su ROI con su logo, no el de la productora.", badge: null },
      { n: "03", title: "Captura de leads por sponsor", body: "Escaneo de credencial → nombre + empresa + email + intereses → CRM HubSpot / Salesforce / Pipedrive con opt-in. Cada conversación queda en el CRM del sponsor.", badge: null },
    ],
  },
  {
    n: "03", tag: "EL ROI POST-EVENTO", dark: false, id: "roi",
    title: "Buscas justificar el presupuesto del próximo congreso.",
    lead: "Encuentras reportes por sponsor + engagement por sesión + liquidación 48-72h. El gerente comercial necesita números para defender el presupuesto del próximo año.",
    heroCount: 0,
    features: [
      { n: "01", title: "ROI por sponsor con cifras concretas", body: "Leads capturados, meetings 1-a-1 confirmados, tiempo en stand, perfil del visitante. El sponsor gold recibe el reporte que justifica renovar el próximo año.", badge: "Reporte por sponsor" },
      { n: "02", title: "Engagement por sesión y por ponente", body: "Asistencia, permanencia, NPS por sesión opcional. Identificás qué charlas convocaron y cuáles no — data para programar el próximo año.", badge: null },
      { n: "03", title: "Liquidación 48-72h y facturación SII", body: "Stripe Connect con split por sponsor o ponente. Facturación electrónica SII configurada. Cero reconciliación manual.", badge: null },
      { n: "04", title: "Soporte 24/7 con account manager dedicado", body: "Mismo equipo que operó el evento, disponible para post-mortem y planificación del próximo año.", badge: "vs Eventbrite chatbot" },
    ],
  },
];

const overview = [
  { tag: "01 · El registro", title: "Forms dinámicos por categoría + acreditación profesional con foto.", href: "#registro" },
  { tag: "02 · El día del evento", title: "Netlink networking 1-a-1 + check-in por sesión + dashboards white-label.", href: "#evento" },
  { tag: "03 · El ROI post-evento", title: "Reportes por sponsor + engagement por sesión + liquidación 48-72h.", href: "#roi" },
];

const whyCards = [
  { icon: "🪪", title: "Acreditación profesional", body: "Foto + QR + credencial impresa con cordón por categoría." },
  { icon: "🤝", title: "Netlink B2B", body: "App de meetings 1-a-1 que convierte tu congreso en plataforma de negocios real." },
  { icon: "📊", title: "ROI medible", body: "Reportes por sponsor, por sesión, por sala. Renovación con datos concretos." },
  { icon: "💰", title: "Sin costos ocultos", body: "Acreditación + Netlink + dashboards + soporte incluidos en el cargo." },
];

const verticals = [
  { icon: "🎪", label: "Festivales" },
  { icon: "🎭", label: "Teatro" },
  { icon: "⚽", label: "Deportes" },
  { icon: "💼", label: "Conferencias" },
  { icon: "🎡", label: "Parques" },
  { icon: "🏟️", label: "Recintos y arenas" },
];

const checklist = [
  "Forms dinámicos por categoría sin licencia extra",
  "Netlink networking app incluido",
  "Dashboards white-label para tus sponsors",
  "Multi-moneda CLP + USD para ponentes internacionales",
  "Acreditación profesional con foto + cordón",
  "Onboarding completo en 4-8 semanas",
];

const faqs = [
  { q: "¿Cómo manejo tarifas distintas por categoría (socio, no socio, prensa, sponsor, C-level)?", a: "Forms condicionales. Cada categoría con sus campos, su tarifa y sus validaciones. Validación automática por RUT empresa para socios de la cámara, dominio corporativo para empresas adheridas, credencial de medio para prensa." },
  { q: "¿Puedo mostrar el dashboard con la marca de mi cliente, no la mía?", a: "Sí. Dashboard white-label opcional con el logo y colores del cliente (cámara, asociación gremial, corporativo). El sponsor ve su ROI con su marca, no con la de la productora." },
  { q: "¿Netlink funciona si mi audiencia es C-level que no instala apps?", a: "Sí. Es web-app — se abre desde el navegador del celular sin descarga del store. Clave para C-level que no instala apps random. Adopción típica: 60-80% de asistentes lo activan. Si necesitás más, hay versión nativa iOS/Android opcional." },
  { q: "¿Pueden cobrar en USD a un ponente internacional?", a: "Sí — multi-moneda nativa. El asistente internacional paga en USD, el local en CLP. La liquidación al ponente internacional va en USD a su cuenta extranjera vía Stripe Connect. Compliance Banco Central resuelto." },
  { q: "¿Qué reporte le entrego al sponsor gold para que renueve?", a: "Reporte ROI por sponsor: leads capturados con datos completos, meetings 1-a-1 confirmados (vía Netlink), tiempo promedio en stand, perfil demográfico del tráfico, NPS si lo activaste. El sponsor recibe el documento que justifica internamente el presupuesto del próximo año." },
];
---
<Site title="Servicios Ticketplus · Congresos y eventos corporativos — Tecnología del registro al ROI">

  <!-- HERO -->
  <header class="biz-hero">
    <div class="container">
      <div class="biz-hero__content">
        <p class="biz-hero__eyebrow">Servicios Ticketplus · Congresos y eventos corporativos</p>
        <h1 class="biz-hero__title">La tecnología que opera tu congreso, del registro al ROI.</h1>
        <p class="biz-hero__lead">Para summits, congresos B2B, seminarios y eventos corporativos. Forms dinámicos por categoría, acreditación con foto + cordón, Netlink networking 1-a-1, dashboards white-label para sponsors y multi-moneda CLP+USD.</p>
        <div class="biz-hero__ctas">
          <a href="#contacto" class="btn btn--primary">Cotizar para mi congreso →</a>
        </div>
      </div>
      <div class="biz-hero__visual">
        <img src={`${import.meta.env.BASE_URL}/verticals/vertical-conferencias.jpg`} alt="Congreso operado con Ticketplus" class="biz-hero__devices" />
      </div>
    </div>
  </header>

  <!-- LOGOS CLOUD -->
  <section class="logos-cloud">
    <div class="container">
      <p class="logos-cloud__label">Congresos y empresas que confían en Ticketplus.</p>
      <div class="logos-cloud__grid">
        {["Endeavor 🇨🇱","ICARE 🇨🇱","CChC 🇨🇱","SOFOFA 🇨🇱","Cámara Comercio 🇨🇱","Singularity University 🇨🇱"].map(l => (
          <div class="logo-tile-dark">{l}</div>
        ))}
      </div>
    </div>
  </section>

  <!-- OVERVIEW DE LOS 3 MOMENTOS -->
  <section class="biz-promises">
    <div class="container">
      <div style="text-align:center; max-width:760px; margin:0 auto 56px;">
        <h2 class="section__title">Tu congreso vive tres frentes.</h2>
        <p class="section__lead" style="color:var(--color-text-muted);">Las herramientas se activan según los necesites.</p>
      </div>
      <div class="biz-promises__grid">
        {overview.map((o) => (
          <a class="promise-card" href={o.href}>
            <span class="promise-card__num">{o.tag}</span>
            <h3 class="promise-card__title">{o.title}</h3>
            <span class="promise-card__link">Ver capacidades →</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- MOMENTO 01 · EL REGISTRO -->
  {(() => {
    const p = moments[0];
    const pi = 0;
    const groups = [{ type: "grid", features: p.features }];
    let gridSeen = 0;
    return (
      <section class={`biz-scene biz-scene--light biz-scene--p${pi}`} id={p.id}>
        <div class="container">
          <header class="biz-scene__header">
            <span class="biz-scene__eyebrow">{p.n} · {p.tag}</span>
            <h2 class="biz-scene__title">{p.title}</h2>
            {p.lead && <p class="biz-scene__lead">{p.lead}</p>}
          </header>

          {groups.map((g) => {
            const gIdx = gridSeen;
            gridSeen++;
            return (
              <div class={`biz-scene__grid biz-scene__grid--${g.features.length}`}>
                {g.features.map((f, fi) => (
                  <article class="scene-card">
                    <div class={`scene-card__stage scene-card__stage--p${pi}-${fi + gIdx * 10}`} aria-hidden="true">
                      <span class="scene-card__chip">{f.n}</span>
                    </div>
                    <div class="scene-card__body">
                      <h4 class="scene-card__title">{f.title}</h4>
                      <p class="scene-card__text">{f.body}</p>
                      {f.badge && <span class="biz-scene__badge">{f.badge}</span>}
                    </div>
                  </article>
                ))}
              </div>
            );
          })}
        </div>
      </section>
    );
  })()}

  <!-- MOMENTO 02 · EL DÍA DEL EVENTO -->
  {(() => {
    const p = moments[1];
    const pi = 1;
    const groups = [{ type: "grid", features: p.features }];
    let gridSeen = 0;
    return (
      <section class={`biz-scene biz-scene--dark biz-scene--p${pi}`} id={p.id}>
        <div class="container">
          <header class="biz-scene__header">
            <span class="biz-scene__eyebrow">{p.n} · {p.tag}</span>
            <h2 class="biz-scene__title">{p.title}</h2>
            {p.lead && <p class="biz-scene__lead">{p.lead}</p>}
          </header>

          {groups.map((g) => {
            const gIdx = gridSeen;
            gridSeen++;
            return (
              <div class={`biz-scene__grid biz-scene__grid--${g.features.length}`}>
                {g.features.map((f, fi) => (
                  <article class="scene-card">
                    <div class={`scene-card__stage scene-card__stage--p${pi}-${fi + gIdx * 10}`} aria-hidden="true">
                      <span class="scene-card__chip">{f.n}</span>
                    </div>
                    <div class="scene-card__body">
                      <h4 class="scene-card__title">{f.title}</h4>
                      <p class="scene-card__text">{f.body}</p>
                      {f.badge && <span class="biz-scene__badge">{f.badge}</span>}
                    </div>
                  </article>
                ))}
              </div>
            );
          })}
        </div>
      </section>
    );
  })()}

  <!-- MÓDULO 05 · NETLINK -->
  <section class="biz-scene biz-scene--dark" id="netlink">
    <div class="container">
      <header class="biz-scene__header">
        <span class="biz-scene__eyebrow">Módulo 05 · NETLINK</span>
        <h2 class="biz-scene__title">Buscas que el congreso o feria sea plataforma de negocios con app mobile de networking.</h2>
        <p class="biz-scene__lead">Encuentras una app mobile nativa de networking 1-a-1 integrada con ticketing.</p>
      </header>
      <div class="biz-scene__grid biz-scene__grid--5">
        <article class="scene-card">
          <div class="scene-card__stage scene-card__stage--p1-0" aria-hidden="true"><span class="scene-card__chip">01</span></div>
          <div class="scene-card__body">
            <h4 class="scene-card__title">Pool de asistentes navegable</h4>
            <p class="scene-card__text">Cada asistente arma su perfil profesional (empresa, cargo, intereses). El resto del pool es navegable y filtrable — el fan deja de ser anónimo.</p>
          </div>
        </article>
        <article class="scene-card">
          <div class="scene-card__stage scene-card__stage--p1-1" aria-hidden="true"><span class="scene-card__chip">02</span></div>
          <div class="scene-card__body">
            <h4 class="scene-card__title">Chat 1-a-1 + agendamiento</h4>
            <p class="scene-card__text">Reuniones agendadas dentro de la app, slots de tiempo, salas asignadas, recordatorios. La feria se convierte en una plataforma de meetings.</p>
            <span class="biz-scene__badge">Meetings agendados</span>
          </div>
        </article>
        <article class="scene-card">
          <div class="scene-card__stage scene-card__stage--p1-2" aria-hidden="true"><span class="scene-card__chip">03</span></div>
          <div class="scene-card__body">
            <h4 class="scene-card__title">Lead capture + notificaciones push</h4>
            <p class="scene-card__text">Escaneo de leads en stand, intercambio de tarjetas digital, notificaciones push para reuniones y novedades de los expositores que el asistente sigue.</p>
          </div>
        </article>
        <article class="scene-card">
          <div class="scene-card__stage scene-card__stage--p1-3" aria-hidden="true"><span class="scene-card__chip">04</span></div>
          <div class="scene-card__body">
            <h4 class="scene-card__title">Galería + repositorio de descargas</h4>
            <p class="scene-card__text">Cada expositor sube fotos, fichas técnicas, brochures y videos. El asistente descarga lo que le interesa sin pedirlo por mail después.</p>
          </div>
        </article>
        <article class="scene-card">
          <div class="scene-card__stage scene-card__stage--p1-4" aria-hidden="true"><span class="scene-card__chip">05</span></div>
          <div class="scene-card__body">
            <h4 class="scene-card__title">Branding + EXTERNALS + control de acceso</h4>
            <p class="scene-card__text">Branding completo del evento. Integraciones con sistemas externos (CRM, plataformas de congresos). Control de acceso a la app por tipo de ticket.</p>
          </div>
        </article>
      </div>
    </div>
  </section>

  <!-- CUENTA CON EL EQUIPO -->
  <EquipoSection />

  <!-- POR QUÉ TICKETPLUS -->
  <section class="why-tp">
    <div class="container">
      <div class="why-tp__header">
        <span class="why-tp__eyebrow">Por qué Ticketplus</span>
        <h2 class="why-tp__title">Lo que recibe tu congreso al elegir Ticketplus.</h2>
        <p class="why-tp__lead">Acreditación + Netlink + reportería ROI + multi-moneda en una sola plataforma. Un solo responsable.</p>
      </div>
      <div class="why-tp__grid">
        {whyCards.map((w) => (
          <article class="why-tp__card">
            <div class="why-tp__icon">{w.icon}</div>
            <h3 class="why-tp__card-title">{w.title}</h3>
            <p class="why-tp__card-body">{w.body}</p>
          </article>
        ))}
      </div>
      <div class="why-tp__cta">
        <a href="#contacto" class="btn btn--primary">Cotizar para mi congreso →</a>
      </div>
    </div>
  </section>

  <!-- CONTACTO -->
  <section style="background:#fafafa; padding:120px 0; border-top:1px solid var(--color-border);" id="contacto">
    <div class="container">
      <div class="form-grid-tp">
        <div>
          <p class="section__eyebrow" style="color:var(--color-secondary);">Hablemos de tu próximo congreso</p>
          <h2 class="section__title">Buscas cotizar. Encuentras un plan armado con tu congreso, audiencia y sponsors.</h2>
          <p class="section__lead" style="color:var(--color-text-muted); margin-bottom:28px;">Cotización en menos de 24 horas hábiles.</p>
          <ul style="list-style:none; padding:0; font-family:var(--font-body); font-size:0.95rem; color:var(--color-text-muted); line-height:2.2;">
            {checklist.map(item => (
              <li><span style="color:#1a8a3a; font-weight:700;">✓</span> {item}</li>
            ))}
          </ul>
        </div>
        <div style="background:#fff; border:1px solid var(--color-border); padding:40px; border-radius:var(--radius-card); box-shadow:var(--shadow-elevated);">
          <h3 style="font-family:var(--font-heading); font-size:1.4rem; font-weight:700; margin:0 0 6px; letter-spacing:-0.02em;">Cotizar para mi congreso</h3>
          <p style="font-family:var(--font-body); font-size:0.9rem; color:var(--color-text-muted); margin:0 0 28px;">Respondemos en menos de 24 horas hábiles.</p>
          {[["Nombre","text","Tu nombre"],["Email corporativo","email","tu@empresa.com"],["Organización","text","Nombre de tu organización"]].map(([label,type,placeholder]) => (
            <div style="margin-bottom:14px;">
              <label style="display:block; font-family:var(--font-heading); font-size:12px; font-weight:600; color:var(--color-text-muted); margin-bottom:6px;">{label}</label>
              <input type={type} placeholder={placeholder} style="width:100%; border:1px solid var(--color-border); background:#fff; color:var(--color-text-primary); padding:12px 14px; border-radius:8px; font-size:14px; font-family:inherit;" />
            </div>
          ))}
          <div style="margin-bottom:14px;">
            <label style="display:block; font-family:var(--font-heading); font-size:12px; font-weight:600; color:var(--color-text-muted); margin-bottom:6px;">Tipo de evento</label>
            <select style="width:100%; border:1px solid var(--color-border); background:#fff; color:var(--color-text-primary); padding:12px 14px; border-radius:8px; font-size:14px; font-family:inherit;">
              <option>Summit corporativo</option>
              <option>Congreso gremial</option>
              <option>Seminario</option>
              <option>Kick-off interno</option>
              <option>Otro</option>
            </select>
          </div>
          <div style="margin-bottom:20px;">
            <label style="display:block; font-family:var(--font-heading); font-size:12px; font-weight:600; color:var(--color-text-muted); margin-bottom:6px;">Asistentes esperados</label>
            <select style="width:100%; border:1px solid var(--color-border); background:#fff; color:var(--color-text-primary); padding:12px 14px; border-radius:8px; font-size:14px; font-family:inherit;">
              <option>Hasta 200</option>
              <option>200-800</option>
              <option>800-2.500</option>
              <option>+2.500</option>
            </select>
          </div>
          <button style="width:100%; background:var(--gradient-brand); color:#fff; padding:14px; border:none; border-radius:var(--radius-button); font-family:var(--font-heading); font-weight:700; font-size:15px; cursor:pointer;">Cotizar para mi congreso →</button>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section style="background:#fff; padding:120px 0; border-top:1px solid var(--color-border);" id="faq">
    <div class="container" style="max-width:820px;">
      <div style="text-align:center; margin-bottom:56px;">
        <p class="section__eyebrow" style="color:var(--color-primary);">Preguntas frecuentes</p>
        <h2 class="section__title">Preguntas frecuentes.</h2>
      </div>
      {faqs.map((f, i) => (
        <details class="faq-item" open={i === 0}>
          <summary class="faq-item__head">
            <span>{f.q}</span><span class="chev" aria-hidden="true">+</span>
          </summary>
          <div class="faq-item__body">
            <p set:html={f.a} />
          </div>
        </details>
      ))}
    </div>
  </section>

  <!-- FINAL CTA -->
  <section style="background:var(--gradient-brand); color:#fff; text-align:center; padding:120px 24px;">
    <div class="container">
      <h2 style="font-family:var(--font-heading); font-size:clamp(2rem,4vw,3rem); font-weight:800; letter-spacing:-0.03em; margin:0 0 14px; line-height:1.1;">Tu congreso. Tus sponsors. Operación probada.</h2>
      <p style="font-family:var(--font-body); font-size:1.1rem; opacity:0.92; margin:0 0 36px;">Cotización en menos de 24 horas hábiles. Sin compromiso.</p>
      <a href="#contacto" style="display:inline-block; background:#fff; color:var(--color-text-primary); padding:16px 40px; border-radius:var(--radius-button); font-family:var(--font-heading); font-weight:700; font-size:1rem; text-decoration:none;">Cotizar para mi congreso →</a>
    </div>
  </section>
</Site>

<style>
  /* ── Biz Hero ─────────────────────────────────────────── */
  .biz-hero {
    background: radial-gradient(ellipse at 70% 20%, #1a0c0c 0%, #050505 60%) #050505;
    color: #fff;
    padding: 120px 0;
    position: relative;
  }
  .biz-hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 56px;
    align-items: center;
  }
  .biz-hero__visual { display: flex; justify-content: center; align-items: center; }
  .biz-hero__devices {
    width: 100%;
    max-width: 620px;
    height: auto;
    display: block;
    border-radius: 16px;
    filter: drop-shadow(0 30px 60px rgba(0,0,0,0.45));
  }
  .biz-hero__eyebrow {
    font-family: var(--font-heading);
    font-size: 11px; font-weight: 600;
    letter-spacing: 0.14em; text-transform: uppercase;
    color: var(--color-secondary);
    margin: 0 0 20px;
  }
  .biz-hero__title {
    font-family: var(--font-heading);
    font-size: clamp(2.2rem, 4.6vw, 4.2rem);
    font-weight: 900; letter-spacing: -0.03em; line-height: 1.05;
    color: #fff; margin: 0 0 24px; max-width: 820px;
  }
  .biz-hero__lead {
    font-family: var(--font-body); font-size: 1.1rem;
    color: rgba(255,255,255,0.70); line-height: 1.65;
    max-width: 560px; margin: 0 0 36px;
  }
  .biz-hero__ctas { display: flex; gap: 12px; flex-wrap: wrap; }

  /* ── Promise cards (TOC 3 momentos) ─────────────────────── */
  .biz-promises { background: #fff; padding: 80px 0 20px; }
  .biz-promises__grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px;
  }
  .promise-card {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-card);
    padding: 36px 32px;
    text-decoration: none; color: inherit;
    display: flex; flex-direction: column; gap: 14px;
    transition: transform .2s, border-color .2s, box-shadow .2s;
  }
  .promise-card:hover {
    transform: translateY(-3px);
    border-color: rgba(10,10,10,0.18);
    box-shadow: 0 12px 28px rgba(0,0,0,0.06);
  }
  .promise-card__num {
    align-self: flex-start;
    font-family: var(--font-heading);
    font-size: 0.7rem; font-weight: 800;
    color: var(--color-primary);
    background: rgba(255,19,19,0.08);
    padding: 5px 12px; border-radius: 999px;
    letter-spacing: 0.14em;
  }
  .promise-card__title {
    font-family: var(--font-heading);
    font-size: 1.4rem; font-weight: 800; letter-spacing: -0.02em; margin: 0;
  }
  .promise-card__link {
    font-family: var(--font-heading); font-size: 0.85rem; font-weight: 700;
    color: var(--color-primary); margin-top: auto;
  }

  /* ── Scenes ────────────────────────────────────────────── */
  .biz-scene { padding: 120px 0; position: relative; overflow: hidden; }
  .biz-scene--light { background: #fff; }
  .biz-scene--dark { background: #0a0a0a; color: #fff; }
  .biz-scene--p0 { background: #fff; }
  .biz-scene--p1 { background: radial-gradient(ellipse at 70% 20%, #1a0c0c 0%, #050505 60%) #050505; color: #fff; }
  .biz-scene--p2 { background: linear-gradient(180deg, #fff 0%, #fafafa 100%); }

  .biz-scene__header { max-width: 760px; margin-bottom: 64px; }
  .biz-scene__eyebrow {
    display: inline-block; font-family: var(--font-heading);
    font-size: 11px; font-weight: 700; letter-spacing: 0.16em; text-transform: uppercase;
    color: var(--color-secondary); margin-bottom: 18px;
  }
  .biz-scene--light .biz-scene__eyebrow { color: var(--color-primary); }
  .biz-scene__title {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4.6vw, 3rem);
    font-weight: 800; letter-spacing: -0.03em; line-height: 1.06; margin: 0 0 20px;
  }
  .biz-scene--dark .biz-scene__title { color: #fff; }
  .biz-scene__lead {
    font-family: var(--font-body); font-size: 1.1rem;
    line-height: 1.6; margin: 0; max-width: 620px;
  }
  .biz-scene--light .biz-scene__lead { color: var(--color-text-muted); }
  .biz-scene--dark .biz-scene__lead { color: rgba(255,255,255,0.72); }

  /* hero composition */
  .biz-scene__hero {
    display: grid; grid-template-columns: 1.15fr 0.85fr;
    gap: 64px; align-items: center; margin-bottom: 72px;
  }
  .biz-scene__hero.is-reverse { grid-template-columns: 0.85fr 1.15fr; }
  .biz-scene__hero.is-reverse .scene-stage { order: 2; }
  .biz-scene__hero.is-reverse .biz-scene__caption { order: 1; }
  .biz-scene__hero + .biz-scene__hero,
  .biz-scene__hero + .biz-scene__grid,
  .biz-scene__grid + .biz-scene__hero { margin-top: 88px; }
  .biz-scene__grid + .biz-scene__grid { margin-top: 28px; }

  .biz-scene__caption-tag {
    display: inline-block; font-family: var(--font-heading);
    font-size: 11px; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase;
    color: var(--color-secondary);
    background: rgba(255,171,36,0.10);
    border: 1px solid rgba(255,171,36,0.28);
    padding: 5px 11px; border-radius: 999px; margin-bottom: 18px;
  }
  .biz-scene--dark .biz-scene__caption-tag { background: rgba(255,171,36,0.14); border-color: rgba(255,171,36,0.36); }
  .biz-scene__caption-title {
    font-family: var(--font-heading);
    font-size: clamp(1.4rem, 2.2vw, 1.85rem);
    font-weight: 800; letter-spacing: -0.02em; line-height: 1.15; margin: 0 0 14px;
  }
  .biz-scene__caption-body {
    font-family: var(--font-body); font-size: 1rem;
    line-height: 1.65; margin: 0 0 18px;
  }
  .biz-scene--light .biz-scene__caption-body { color: var(--color-text-muted); }
  .biz-scene--dark .biz-scene__caption-body { color: rgba(255,255,255,0.72); }

  .biz-scene__badge {
    display: inline-block; font-family: var(--font-heading);
    font-size: 0.7rem; font-weight: 800;
    color: var(--color-secondary);
    background: rgba(255,171,36,0.12);
    border: 1px solid rgba(255,171,36,0.30);
    border-radius: 4px; padding: 4px 10px;
    letter-spacing: 0.06em; text-transform: uppercase; margin-top: 4px;
  }
  .biz-scene--dark .biz-scene__badge { background: rgba(255,171,36,0.15); border-color: rgba(255,171,36,0.40); }

  /* ── Stage (hero mock containers) ──────────────────────── */
  .scene-stage {
    position: relative; border-radius: 16px; overflow: hidden;
    box-shadow: 0 30px 80px rgba(0,0,0,0.18);
    aspect-ratio: 4/3.1;
    background: #f3f3f3;
    border: 1px solid rgba(10,10,10,0.06);
  }
  .biz-scene--dark .scene-stage {
    background: #141414;
    border-color: rgba(255,255,255,0.08);
    box-shadow: 0 30px 80px rgba(0,0,0,0.5);
  }
  .scene-stage--showpage { background: linear-gradient(180deg, #f8f7f5 0%, #ecebe7 100%); }
  .scene-stage--venue { background: linear-gradient(160deg, #1f1408 0%, #0a0a0a 100%); }

  /* ── Supporting scenes grid (scene-cards) ──────────────── */
  .biz-scene__grid {
    display: grid; grid-template-columns: repeat(6, 1fr); gap: 20px;
  }
  .scene-card {
    grid-column: span 2;
    background: #fff;
    border: 1px solid rgba(10,10,10,0.08);
    border-radius: 14px; overflow: hidden;
    display: flex; flex-direction: column;
    transition: transform .2s, box-shadow .2s, border-color .2s;
  }
  .biz-scene--dark .scene-card { background: rgba(255,255,255,0.03); border-color: rgba(255,255,255,0.08); }
  .scene-card:hover { transform: translateY(-3px); box-shadow: 0 14px 36px rgba(0,0,0,0.08); border-color: rgba(10,10,10,0.18); }
  .biz-scene--dark .scene-card:hover { border-color: rgba(255,255,255,0.18); box-shadow: 0 14px 36px rgba(0,0,0,0.5); }
  .biz-scene__grid--2 .scene-card { grid-column: span 3; }
  .biz-scene__grid--3 .scene-card { grid-column: span 2; }
  .biz-scene__grid--4 .scene-card { grid-column: span 3; }

  .scene-card__stage {
    aspect-ratio: 16/9;
    position: relative;
    background: linear-gradient(135deg, rgba(255,19,19,0.10) 0%, rgba(255,171,36,0.10) 100%);
    overflow: hidden;
  }
  .scene-card__stage::before,
  .scene-card__stage::after {
    content: ''; position: absolute; border-radius: 50%;
    filter: blur(20px); opacity: 0.55;
  }
  .scene-card__stage::before {
    width: 60%; height: 80%; top: -10%; left: -10%;
    background: radial-gradient(circle, var(--color-primary) 0%, transparent 70%);
  }
  .scene-card__stage::after {
    width: 50%; height: 60%; bottom: -10%; right: -10%;
    background: radial-gradient(circle, var(--color-secondary) 0%, transparent 70%);
  }
  .biz-scene--dark .scene-card__stage {
    background: linear-gradient(135deg, rgba(255,19,19,0.18) 0%, rgba(255,171,36,0.18) 100%);
  }
  /* tints por momento */
  .scene-card__stage--p0-0 { background: linear-gradient(135deg, #fff1e0 0%, #ffd7b8 100%); }
  .scene-card__stage--p0-1 { background: linear-gradient(135deg, #fde6e6 0%, #fff5cc 100%); }
  .scene-card__stage--p0-2 { background: linear-gradient(135deg, #fff5cc 0%, #ffe0d4 100%); }
  .scene-card__stage--p1-0,
  .scene-card__stage--p1-1,
  .scene-card__stage--p1-2,
  .scene-card__stage--p1-10,
  .scene-card__stage--p1-11,
  .scene-card__stage--p1-12 { background: linear-gradient(135deg, #2a1414 0%, #0c0c0c 100%); }
  .scene-card__stage--p2-0 { background: linear-gradient(135deg, #fff 0%, #fff3c4 100%); }
  .scene-card__stage--p2-1 { background: linear-gradient(135deg, #fff0e8 0%, #fde6e6 100%); }
  .scene-card__stage--p2-2 { background: linear-gradient(135deg, #fde6e6 0%, #fff5cc 100%); }
  .scene-card__stage--p2-3 { background: linear-gradient(135deg, #fff5cc 0%, #ffe0d4 100%); }

  .scene-card__chip {
    position: absolute; z-index: 2;
    top: 14px; left: 14px;
    font-family: var(--font-heading);
    font-size: 11px; font-weight: 800; letter-spacing: 0.08em;
    background: #fff; color: var(--color-text-primary);
    padding: 5px 10px; border-radius: 999px;
    border: 1px solid rgba(10,10,10,0.08);
  }
  .biz-scene--dark .scene-card__chip { background: rgba(20,20,20,0.85); color: #fff; border-color: rgba(255,255,255,0.12); }

  .scene-card__body { padding: 24px 24px 28px; display: flex; flex-direction: column; gap: 8px; }
  .scene-card__title {
    font-family: var(--font-heading);
    font-size: 1.1rem; font-weight: 800; letter-spacing: -0.02em;
    margin: 0; line-height: 1.25;
  }
  .biz-scene--dark .scene-card__title { color: #fff; }
  .scene-card__text {
    font-family: var(--font-body); font-size: 0.9rem; line-height: 1.6; margin: 0;
  }
  .biz-scene--light .scene-card__text { color: var(--color-text-muted); }
  .biz-scene--dark .scene-card__text { color: rgba(255,255,255,0.7); }

  /* ── Logos cloud ───────────────────────────────────────── */
  .logos-cloud { background:#0a0a0a; padding:60px 0; border-top:1px solid rgba(255,255,255,0.06); }
  .logos-cloud__label { font-family:var(--font-heading); font-size:11px; color:rgba(255,255,255,0.38); letter-spacing:0.14em; text-transform:uppercase; font-weight:600; text-align:center; margin-bottom:32px; }
  .logos-cloud__grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(130px,1fr)); gap:16px; }
  .logo-tile-dark { aspect-ratio:3/1.2; background:rgba(255,255,255,0.04); border:1px solid rgba(255,255,255,0.10); border-radius:8px; display:flex; align-items:center; justify-content:center; color:rgba(255,255,255,0.60); font-size:0.82rem; font-weight:700; font-family:var(--font-heading); }

  /* ── Spotlight ─────────────────────────────────────────── */
  .spotlight { padding: 100px 0; border-top: 1px solid var(--color-border); }
  .spotlight--brand { background: var(--gradient-brand); color: #fff; border-top: none; }
  .spotlight--dark {
    background: radial-gradient(ellipse at 50% 30%, #1a0c0c 0%, #050505 60%) #050505;
    color: #fff; border-top: none;
  }
  .spotlight .container { text-align: center; }
  .spotlight__eyebrow {
    display: inline-block; font-family: var(--font-heading);
    font-size: 11px; font-weight: 700; letter-spacing: 0.16em; text-transform: uppercase;
    padding: 5px 14px; border-radius: 999px;
    margin-bottom: 22px;
    background: rgba(255,255,255,0.16); color: #fff;
  }
  .spotlight--dark .spotlight__eyebrow {
    background: rgba(255,171,36,0.14);
    border: 1px solid rgba(255,171,36,0.36);
    color: var(--color-secondary);
  }
  .spotlight__title {
    font-family: var(--font-heading);
    font-size: clamp(1.6rem, 3.4vw, 2.4rem);
    font-weight: 800; letter-spacing: -0.025em; line-height: 1.15;
    margin: 0 auto 22px; max-width: 880px; color: #fff;
  }
  .spotlight__body {
    font-family: var(--font-body); font-size: 1.05rem;
    line-height: 1.65; margin: 0 auto; max-width: 720px;
    color: rgba(255,255,255,0.94);
  }
  .spotlight--dark .spotlight__body { color: rgba(255,255,255,0.78); }
  .spotlight__cards {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 18px; margin-top: 56px; text-align: left;
  }
  .spotlight-card {
    background: rgba(255,255,255,0.14);
    border: 1px solid rgba(255,255,255,0.22);
    border-radius: 14px; padding: 24px 24px 28px;
    display: flex; flex-direction: column; gap: 10px;
    transition: transform .2s, border-color .2s, background .2s;
  }
  .spotlight--dark .spotlight-card {
    background: rgba(255,255,255,0.04);
    border-color: rgba(255,255,255,0.10);
  }
  .spotlight-card:hover {
    transform: translateY(-3px);
    border-color: rgba(255,255,255,0.4);
    background: rgba(255,255,255,0.20);
  }
  .spotlight--dark .spotlight-card:hover {
    border-color: rgba(255,171,36,0.40);
    background: rgba(255,255,255,0.07);
  }
  .spotlight-card__title {
    font-family: var(--font-heading);
    font-size: 1.05rem; font-weight: 800; letter-spacing: -0.02em; line-height: 1.25;
    margin: 0; color: #fff;
  }
  .spotlight-card__text {
    font-family: var(--font-body); font-size: 0.88rem;
    line-height: 1.55; margin: 0; color: rgba(255,255,255,0.92);
  }
  .spotlight--dark .spotlight-card__text { color: rgba(255,255,255,0.72); }
  @media (max-width: 768px) { .spotlight__cards { grid-template-columns: 1fr; } }
  /* ── Por qué Ticketplus ────────────────────────────────── */
  .why-tp {
    background: linear-gradient(180deg, #fafafa 0%, #fff 100%);
    padding: 120px 0; border-top: 1px solid var(--color-border);
  }
  .why-tp__header { max-width: 760px; margin: 0 auto 56px; text-align: center; }
  .why-tp__eyebrow {
    display: inline-block; font-family: var(--font-heading);
    font-size: 11px; font-weight: 700; letter-spacing: 0.16em; text-transform: uppercase;
    color: var(--color-secondary); margin-bottom: 18px;
  }
  .why-tp__title {
    font-family: var(--font-heading);
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    font-weight: 800; letter-spacing: -0.03em; line-height: 1.1;
    margin: 0 0 16px; color: #0a0a0a;
  }
  .why-tp__lead {
    font-family: var(--font-body); font-size: 1.05rem;
    color: var(--color-text-muted); margin: 0; line-height: 1.55;
  }
  .why-tp__grid {
    display: grid; grid-template-columns: repeat(4, 1fr);
    gap: 20px; margin-bottom: 48px;
  }
  .why-tp__card {
    background: #fff;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-card);
    padding: 32px 26px;
    display: flex; flex-direction: column; gap: 12px;
    transition: transform .2s, border-color .2s, box-shadow .2s;
  }
  .why-tp__card:hover {
    transform: translateY(-3px);
    border-color: rgba(10,10,10,0.18);
    box-shadow: 0 12px 28px rgba(0,0,0,0.06);
  }
  .why-tp__icon {
    font-size: 28px; line-height: 1;
    width: 52px; height: 52px;
    display: flex; align-items: center; justify-content: center;
    background: linear-gradient(135deg, rgba(255,19,19,0.08), rgba(255,171,36,0.10));
    border-radius: 12px;
  }
  .why-tp__card-title {
    font-family: var(--font-heading);
    font-size: 1.05rem; font-weight: 800;
    letter-spacing: -0.02em; line-height: 1.25; margin: 0;
  }
  .why-tp__card-body {
    font-family: var(--font-body); font-size: 0.9rem;
    color: var(--color-text-muted); line-height: 1.55; margin: 0;
  }
  .why-tp__cta { text-align: center; }

  /* ── Verticales ────────────────────────────────────────── */
  .verticals {
    background: #fff;
    padding: 120px 0;
    border-top: 1px solid var(--color-border);
  }
  .verticals__header { max-width: 760px; margin: 0 auto 56px; text-align: center; }
  .verticals__eyebrow {
    display: inline-block; font-family: var(--font-heading);
    font-size: 11px; font-weight: 700; letter-spacing: 0.16em; text-transform: uppercase;
    color: var(--color-primary); margin-bottom: 18px;
  }
  .verticals__title {
    font-family: var(--font-heading);
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    font-weight: 800; letter-spacing: -0.03em; line-height: 1.1;
    margin: 0 0 16px; color: #0a0a0a;
  }
  .verticals__grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px;
  }
  .verticals__card {
    background: #fff;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-card);
    padding: 28px 26px;
    display: flex; align-items: center; gap: 16px;
    transition: transform .2s, border-color .2s, box-shadow .2s;
  }
  .verticals__card:hover {
    transform: translateY(-3px);
    border-color: rgba(10,10,10,0.18);
    box-shadow: 0 12px 28px rgba(0,0,0,0.06);
  }
  .verticals__icon {
    font-size: 28px; line-height: 1;
    width: 52px; height: 52px;
    display: flex; align-items: center; justify-content: center;
    background: linear-gradient(135deg, rgba(255,19,19,0.08), rgba(255,171,36,0.10));
    border-radius: 12px;
    flex-shrink: 0;
  }
  .verticals__label {
    font-family: var(--font-heading);
    font-size: 1rem; font-weight: 800;
    letter-spacing: -0.02em; line-height: 1.25; margin: 0;
    color: var(--color-text-primary);
  }

  /* ── Form grid ─────────────────────────────────────────── */
  .form-grid-tp { display:grid; grid-template-columns:1fr 1fr; gap:80px; align-items:center; }
  .section__eyebrow {
    display: inline-block; font-family: var(--font-heading);
    font-size: 11px; font-weight: 700; letter-spacing: 0.14em; text-transform: uppercase;
    margin-bottom: 18px;
  }
  .section__title {
    font-family: var(--font-heading);
    font-size: clamp(2rem, 4vw, 2.8rem);
    font-weight: 800; letter-spacing: -0.03em; line-height: 1.1;
    margin: 0 0 16px;
  }
  .section__lead {
    font-family: var(--font-body); font-size: 1.05rem; line-height: 1.6; margin: 0;
  }

  /* ── FAQ ──────────────────────────────────────────────── */
  .faq-item { border-bottom: 1px solid var(--color-border); padding: 22px 0; }
  .faq-item__head {
    list-style: none; cursor: pointer;
    display: flex; justify-content: space-between; align-items: center;
    gap: 16px;
    font-family: var(--font-heading);
    font-size: 1.05rem; font-weight: 700; letter-spacing: -0.01em;
    color: var(--color-text-primary);
  }
  .faq-item__head::-webkit-details-marker { display: none; }
  .faq-item__head::marker { display: none; content: ''; }
  .faq-item__head .chev {
    font-family: var(--font-heading);
    font-size: 1.6rem; font-weight: 400;
    color: var(--color-secondary);
    transition: transform .2s ease; line-height: 1;
  }
  .faq-item[open] .faq-item__head .chev { transform: rotate(45deg); }
  .faq-item__body { padding-top: 14px; }
  .faq-item__body p {
    font-family: var(--font-body); font-size: 0.95rem;
    color: var(--color-text-muted); line-height: 1.65; margin: 0;
  }
  .faq-item__body p strong { color: var(--color-text-primary); font-weight: 700; }

  /* ── Responsive ────────────────────────────────────────── */
  @media (max-width: 1024px) {
    .biz-hero .container { grid-template-columns: 1fr; gap: 48px; }
    .biz-promises__grid { grid-template-columns: 1fr; }
    .biz-scene__hero { grid-template-columns: 1fr; gap: 32px; }
    .biz-scene__hero.is-reverse { grid-template-columns: 1fr; }
    .biz-scene__hero.is-reverse .scene-stage { order: initial; }
    .biz-scene__hero.is-reverse .biz-scene__caption { order: initial; }
    .biz-scene__grid,
    .biz-scene__grid--3,
    .biz-scene__grid--4 { grid-template-columns: repeat(2, 1fr); }
    .scene-card,
    .biz-scene__grid--3 .scene-card,
    .biz-scene__grid--4 .scene-card { grid-column: span 1; }
    .why-tp__grid { grid-template-columns: repeat(2, 1fr); }
    .verticals__grid { grid-template-columns: repeat(2, 1fr); }
    .form-grid-tp { grid-template-columns: 1fr; gap: 48px; }
  }
  @media (max-width: 768px) {
    .biz-scene { padding: 80px 0; }
    .biz-scene__grid,
    .biz-scene__grid--3,
    .biz-scene__grid--4 { grid-template-columns: 1fr; }
    .why-tp__grid { grid-template-columns: 1fr; }
    .verticals__grid { grid-template-columns: 1fr; }    .spotlight { padding: 72px 0; }
  }
</style>
